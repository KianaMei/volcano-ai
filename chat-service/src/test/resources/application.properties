# Test Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=wcwl1234!
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=5

# MyBatis Plus
mybatis-plus.configuration.map-underscore-to-camel-case=true

# Redis Configuration (required by CozeAccessTokenProvider)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Coze Config (mock values for test)
coze.oauth-client-id=test
coze.oauth-public-key-id=test
coze.oauth-private-key-path=
